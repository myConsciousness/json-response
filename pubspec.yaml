name: json_pro
description: A most easily usable JSON wrapper library in Dart. With JsonPro, you can easily and safely handle JSON on your application.
version: 1.0.0
homepage: https://github.com/myConsciousness/json-pro

environment:
  sdk: ">=2.15.1 <3.0.0"

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  import_sorter: ^4.6.0
